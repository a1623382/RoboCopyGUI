<Window
    x:Class="RoboCopyGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RoboCopyGUI"
    xmlns:vm="using:RoboCopyGUI.ViewModels"
    xmlns:m="using:RoboCopyGUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleArea" Grid.Row="0" Height="48" Padding="16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                <FontIcon Glyph="&#xE8F1;" FontSize="20" />
                <TextBlock Text="RoboCopy GUI" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
            </StackPanel>

            <CommandBar Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"
                        DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
                <AppBarButton Icon="Add" Label="Add Task" />
                <AppBarButton Icon="Play" Label="Start Queue" />
                <AppBarButton Icon="Stop" Label="Cancel" />
                <AppBarSeparator />
                <AppBarButton Icon="Save" Label="Save Queue" />
                <AppBarButton Icon="Setting" Label="Settings" />
            </CommandBar>
        </Grid>

        <Grid Grid.Row="1" Padding="16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" MinWidth="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" CornerRadius="8"
                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1" Padding="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Task Queue" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,0,12" />

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <AutoSuggestBox Grid.Row="0" PlaceholderText="Source path..." QueryIcon="Folder" Margin="0,0,0,8" />
                    <AutoSuggestBox Grid.Row="1" PlaceholderText="Destination path..." QueryIcon="SyncFolder" />
                </Grid>

                <ListView Grid.Row="2" Padding="0" SelectionMode="Single" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="8,6" CornerRadius="6" Margin="0,0,0,4">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                                        Width="4" CornerRadius="2" Margin="0,0,8,0"
                                        Background="Gray" />

                                <TextBlock Grid.Row="0" Grid.Column="1"
                                           Text="Source Path"
                                           Style="{StaticResource BodyStrongTextBlockStyle}"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Row="1" Grid.Column="1"
                                           Text="Destination Path"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis" FontSize="12" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                    <Button Content="Move Up" />
                    <Button Content="Move Down" />
                    <Button Content="Duplicate" />
                    <Button Content="Remove" />
                </StackPanel>
            </Grid>

            <TextBlock Grid.Column="1" Text="Right Panel" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>

        <TextBlock Grid.Row="2" Text="Status: Ready" FontSize="12" Padding="16,4" />
    </Grid>
</Window>
